const body = document.body;
const header = document.querySelector('header.header');
const headerScrollClass = 'header-scroll';
const html = document.documentElement;

export { body, header, headerScrollClass, html };
